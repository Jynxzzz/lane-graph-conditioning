seed: 42

model:
  name: tf_lane_cond
  input_dim: 2
  d_model: 128
  nhead: 4
  num_layers: 2
  dim_feedforward: 256
  dropout: 0.1
  future_len: 80
  use_neighbors: true
  neighbor_hidden_dim: 64
  lane_hidden_dim: 128
  n_mp_layers: 2

data:
  scene_list: /home/xingnan/scenario-dreamer/scene_list_123k_signal_ssd.txt
  history_len: 11
  future_len: 80
  max_neighbors: 10
  neighbor_distance: 30.0
  anchor_frames: [10]
  val_ratio: 0.15
  max_lanes: 16
  lane_points: 10
  expand_anchors: false
  augment_rotation: true

training:
  batch_size: 128
  lr: 0.0005
  weight_decay: 0.01
  max_epochs: 100
  gradient_clip_val: 1.0
  num_workers: 8
  check_val_every_n_epoch: 5
  log_every_n_steps: 50
  warmup_epochs: 5

output_dir: /mnt/hdd12t/outputs/scenario_dreamer_big_v1
